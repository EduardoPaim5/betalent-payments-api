meta {
  name: List Clients
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/clients?per_page=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}
