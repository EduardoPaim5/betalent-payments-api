meta {
  name: Show Transaction
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/transactions/{{transactionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}
