meta {
  name: Show Client
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/clients/{{clientId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}
